# 4주차 발표자 노트 — 백업

> **정본이 아니다. 정본은 `.pptx` 이고, `#217` 이 스크립트로 흡수하면 이 파일은
> 사라진다. 지우는 책임은 `#217` 이 진다.**

## 왜 있나

**노트는 저장소에 원본이 0개였다.** 말할 순서·예상 질문·안 할 말이 **어디에도
없었고**, 실제로 한 번 잃었다 — `.pptx` 의 XML 을 다시 압축하다 **11장 중 10장이
날아갔다**(`#217`). 복구된 것은 운이다.

**다른 것과 성격이 다르다.** 문안은 `submissions/README.md` 에, 측정 서술은
`concurrency-spec.md` 7.2.3 · 7.2.4 와 `m3-conclusion.md` 에 원본이 있다. **노트만
사본이 하나뿐이었다.**

## 드리프트를 막는 규칙

> **노트를 고치면 같은 차례에 이 파일도 고친다.**

`.pptx` 는 최건이 고치므로 **문안이 갈 때 같이 간다.** 한쪽만 고치면 다음에
어느 쪽이 맞는지 알 수 없다 — `erd.md` 4절이 경계한 「같은 사실을 두 곳에서
지키면 어긋난다」이고, **이 파일은 그 위험을 알면서 감수한 것**이다. 감수하는
이유는 위의 「원본이 0개」다.

## 뽑은 자리

`docs/submissions/4주차_발표_수정본 (2).pptx` — **노트 11장이 붙어 있는 판이다.**

> ⚠ **같은 디렉토리에 `.pptx` 가 넷이고 그중 노트가 성한 것은 하나다.**
> `4주차 발표.pptx`(이름이 정본처럼 보이는 쪽)는 **노트가 0장**이다 — 잃었던
> 그 판이다. `#217` 이 정본을 하나로 정하기 전까지 **파일 이름으로 최신을
> 고르면 안 된다.**

| 파일 | 노트 |
|---|---|
| `4주차 발표.pptx` | **0장** — 손상된 판 |
| `4주차_발표_수정본.pptx` | 1장 |
| `4주차_발표_수정본 (1).pptx` | 11장 |
| **`4주차_발표_수정본 (2).pptx`** | **11장 — 이 파일의 출처** |

## 지금 낡은 곳 — `.pptx` 쪽을 고쳐야 한다

**아래는 뽑은 그대로 옮긴 것이라 낡은 문장이 그대로 들어 있다.** 백업이지
정정본이 아니다.

| 어디 | 무엇 | 근거 |
|---|---|---|
| 슬라이드 9 | *"재판매 최소가 18ms, 요청 하나가 163ms다. 조건은 섰을 것으로 보이나"* | **163ms 는 취소 지연이 아니다** — 네 요청 종류가 섞인 값이다. 「모른다」가 맞다(`#220`) |
| 슬라이드 9 | *"재려면 취소 경로에 계측이 필요하고 그건 `#106` 에 닿는다"* | **타이머는 쓰기가 아니다.** 막혀서 안 한 것이 아니라 안 한 것이다(`#220`) |
| 슬라이드 3 · 9 | *"175건 통과 + 보류 1건"* | `#221` 이 머지되면 **176건 통과, 보류 0** |
| 슬라이드 10 | *"남은 것은 `#209` 수정과 전 구간 한 번 기동"* | `#221` 이 머지되면 **전 구간 기동만 남는다** |

---

## 노트

### 슬라이드 1

```
표지 — 4주차 주간 발표.
「지난주에 클라우드 배포에 착수했고, 이번 주는 만든 것보다 안 만든 것을 정한 주였습니다」로 연다.
```

### 슬라이드 2

```
01 이번 주 — 닷새 동안 여섯. 타임라인만 훑고 넘어간다.
9/15 화면(#192) · 9/17 계정(#188) · 9/18 배포(#42)·래퍼 · 9/19 HTTPS(#204)·자동화.
말할 것: 단계를 셋으로 가른 이유 — 앞단(VPC·보안그룹·인증서)이 틀리면 무거운 것(RDS·ALB)을 세우기 전에 드러난다.
넘어가는 말: 「안 만든 것」 넷은 배포 하나 안에 있다 → 5번.
```

### 슬라이드 3

```
02 화면 — 새 기능이 아니다. 서버가 이미 아는 값을 화면이 버리고 있던 자리.
검표 37px→46px · 목록 카드에 상태 배지 · 배치도 배지 토큰 재사용.
핵심 한 줄: 검표 응답이 좌석번호를 안 줬다 — 티켓 번호만으로는 줄 선 사람 중 누구인지 모른다.
테스트 수는 이 장에 안 적는다 — 자료 안에서 한 곳(9번)에만 적는 규칙(#219). 물어보면 175건 통과 + 보류 1건이라고 답한다.
```

### 슬라이드 4

```
03 계정 경계 — 이 구성에서 두 사람이 갈리는 유일한 자리.
AWS는 박태준, Cloudflare(도메인·Zero Trust·Access)는 최건.
핵심: IAM에 서로 초대하면 경계가 사라지고 되돌리기 어렵다. 대신 「DNS 레코드를 쓴다」 하나만 되는 토큰을 넘겼다.
실제로 걸린 증거: Access 앱이 있는지 API로 못 봐서 엣지에 직접 물어 확인했다.
```

### 슬라이드 5

```
04 판단 넷 — 전부 「안 만든 것」이다. 이 장이 이번 주의 본체.
NAT 없음 / ALB는 Cloudflare 게시 대역만 (수가 고정이 아니다 — 목록을 apply 때 받아 온다) / 헬스체크는 /api/health / 배포 전략은 pessimistic (ElastiCache 뺌).
대가를 같이 말한다: 줄인 만큼 남은 것 하나하나가 유일한 방어가 된다. 보안그룹이 뚫리면 대안이 없고, 헬스체크가 틀리면 타겟이 전부 죽는다.
/actuator를 안 쓰는 이유가 제일 설명이 필요하다 — Redis 없으면 503이라 전략을 바꾸는 순간 타겟이 전부 죽는다.
발표 전 확인: test_cidr 이 비어 있어야 한다. 열어 둔 채 발표하면 ALB 직행이 열리고 admin 의 Access 가 무의미해진다(#218).
```

### 슬라이드 6

```
아키텍처 전체 도식 — 번호 없는 전면 도판. 5번의 판단 넷이 실제로 어디에 놓이는지 보여주는 자리.
왼쪽이 최건 계정(Cloudflare), 오른쪽이 박태준 계정(AWS). 경계를 건너는 것은 DNS 하나.
짚을 것 둘: NAT Gateway 없음 · 보안그룹 Cloudflare 15개 대역. 나머지는 눈으로 훑게 두고 오래 머물지 않는다.
```

### 슬라이드 7

```
05 실제로 올려서 확인 — 넷을 쟀다.
태스크 둘 healthy(배지 04d65c9a / a320016f) · 관리자 화면만으로 시드 · 동시 홀드 20건에 성공 1·409 거절 19 · 노출 스위치(화면 200, /demo/race 404).
판정 하나를 뒤집었다: AdminCatalogService가 회차를 만들 때 재고·할당량을 함께 채우므로 시드용 EC2가 필요 없다.
배지가 갈린다는 것이 「앱 2대」의 유일한 증거 — 태스크 정의는 하나다.
왜 태스크가 둘인가 — 물어볼 자리다.
1대면 이 프로젝트의 결론이 안 선다. 락이 JVM 안(synchronized)에 있어도 초과 확정이 0으로 나오기 때문이다. 인스턴스가 둘이어야 «락은 프로세스 밖에 있어야 듣는다»가 드러나고, 그게 M3 결론(정합성의 최종 책임은 DB 제약·조건부 UPDATE)의 전제다. 시연도 같은 이유로 2대다(#121).
아닌 것 둘: 가용성이 아니다 — RDS가 Single-AZ라 DB가 단일 장애점이고, 서브넷 2AZ는 ALB 요구사항이다. 성능도 아니다 — 1차 60회는 로컬이고 AWS는 «배포했다»를 보이는 별개 목적이다(infra-decision 4절).
```

### 슬라이드 8

```
06 오리진까지 암호화 — Cloudflare가 거저 주는 TLS는 절반이다.
왼쪽은 우리가 한 일이 아니라 안 고른 선택지다(Flexible이면 엣지↔ALB가 평문). 실제로는 Full에서 Full (strict)로 고정하고 자동 모드를 껐다.
평문으로 지나간 적은 없다 — 오리진으로 가는 레코드가 HTTPS와 같은 날 처음 생겼다.
503이 성공 신호인 이유를 꼭 말한다: 인증서를 거부했다면 526, 보안그룹이 막았다면 무응답. TLS는 통했고 뒤에 태스크가 없었을 뿐이다.
→ 그래서 7번의 확인과 이 확인은 같은 순간이 아니다. 10번의 90%가 여기서 나온다.
```

### 슬라이드 9

```
07 피드백이 작업이 됐다 — 3주차 지적이 그대로 이슈 넷.
CS-6(#191) · CS-4(#200) · 경합도 그래프(#198) · 근거 교체(#197). 넷 다 최건, 같은 기간 박태준은 배포.
부제가 답하는 것: 교수 제안을 성능 축이 아니라 정합성 축으로 받았다. 요청 종류가 둘 이상이 되면 p95가 «홀드+확정의 성능»이 아니라 혼합 워크로드의 성능이 되어 1차 60회가 비교 대상을 잃는다. 그래서 2차는 p95도 TPS도 재지 않는다.
한 줄 요약 — 1차는 다섯 전략의 응답시간을 비교한 것, 2차는 한 전략으로 아무도 지나간 적 없는 코드 경로를 일부러 밟아 깨지는지만 본 것. 비교가 아니라 밟기다.
무엇을 밟았나
· CS-6 (1인 최대 매수) — 같은 사람이 다른 좌석 둘을 동시에 노리면 좌석 락이 못 막는다. 막는 건 할당량 행 하나다. 좌석 1000 · VU 500 · 사용자 20명 · 120초 × 3회. 요청 147만 건, 할당량 거절 70%. 위반 0.
사용자를 20명으로 둔 게 전부다 — VU 25개가 한 사람을 공유해 같은 할당량 행을 계속 다툰다. 1차는 정반대(사용자 > VU)라 이 구역이 구조적으로 안 밟혔다.
· CS-4 (취소와 재선점) — 검증 쿼리도 부하 경로도 동시성 테스트도 셋 다 없던 자리. 좌석 200 · VU 500 · 사용자 500 · 120초 × 3회. 취소 쌍 15만 · 재선점 20만. 위반 0.
무엇을 보고 «0»이라 했나 — 둘 다 끝 상태를 안 센다. 구간이 겹쳤는지를 본다.
· CS-6 — 사용자별 동시 보유 최댓값이 세 회차 모두 정확히 4 = 상한. 닿았으니 밟은 것이고, 안 넘었으니 지켜진 것. 둘 중 하나만으로는 판정이 안 된다.
· CS-4 — 좌석별로 두 사람이 동시에 확정 보유한 구간이 있었나. 0.
★ 쿼리가 진짜 잡는지를 먼저 검사했다. 0이 나오면 «안 깨졌다»인지 «못 본다»인지 모르기 때문이다. 위반 셋을 일부러 심고 세 검사가 각각 잡는지 본 뒤 롤백했다. 셋 다 잡았고, 같은 위반에 기존 V-1 쿼리는 0을 냈다 — 새 쿼리를 만든 이유가 그 대조로 증명된다.
폐기 조건(재선점 0이면 버린다 · 상한에 안 닿으면 버린다)은 재기 전에 정했고, 여섯 회차 전부 넘겨서 발동 안 했다.
★ 파일럿이 설계를 두 번 뒤집었다 — 여기가 제일 중요하다.
· CS-6 — 처음엔 끝난 뒤 «지금 몇 개 들고 있나»를 세려 했다. 그러면 위반이 있어도 0이 나온다. 잡았다 바로 놓으니 끝 상태에 흔적이 없다.
· CS-4 — «좌석이 적을수록 경합이 세다»가 틀렸다. 20분의 1로 줄여도 재판매 최소 시간이 안 움직였다(17ms → 13ms). 다시 팔리려면 홀드·확정 두 왕복이 필요해서다.
→ 파일럿을 안 돌렸으면 0을 받아 들고 통과라고 적었을 것이고, 그 0은 판정이 아니라 눈을 감은 것이었다.
본체는 결함 하나 — #209. 겹친 취소가 할당량을 여러 번 깎아 상한을 우회할 수 있다. 보유량 4에서 좌석 1개짜리 예약을 동시 취소하면 2스레드에 4→2, 12스레드에 4→0. 정답은 3이다.
부하가 못 보는 이유: 그 시나리오는 가상 사용자 하나가 실사용자 하나를 독점해(userPool = vus) 보유량이 0과 1을 못 벗어나고, 두 번 깎여도 0에 눌려 정답과 구분이 안 된다.
→ «부하를 더 주면 보이지 않나요»가 나오면: 안 된다. 손잡이를 돌려서 열리는 게 아니라 하네스의 사용자 배정을 바꿔야 한다. 단위 경합 테스트가 잡았다.
175건 통과에 1건 보류 — 그 1건이 #209이고, 깨진 것을 통과로 적지 않았다는 뜻이다.
못 잰 것 — 먼저 말하는 편이 낫다.
확실히 밟은 것은 취소 둘이 겹치는 구간(15만 번). 모르는 것은 «돌아온 좌석을 남이 먼저 가져가는» 구간이다 — 열리려면 취소 지연 > 재판매 간격이어야 하는데 재판매 최소가 18ms, 요청 하나가 163ms다. 조건은 섰을 것으로 보이나 이 하네스에 관측 장치가 없다. 재려면 취소 경로에 계측이 필요하고 그건 #106에 닿는다.
어디 있나 — concurrency-spec 7.2.3(CS-6)·7.2.4(CS-4) · load-test/scenarios/quota.js·cancel.js · sql/verify-quota.sql·verify-cancel.sql·verify-cancel-selfcheck.sql · CancelConcurrencyTest.java · 7.3 각주(부하가 못 보는 이유) · m3-conclusion 2.5·3.5
```

### 슬라이드 10

```
08 현재 그리고 다음 — 마지막 장. 회수를 여기서 명시한다.
막대: 분석·설계·동시성/1차 측정·시스템 구현 100%, 클라우드 배포 90%.
90%인 이유: 엣지와 오리진을 따로 쟀고 둘이 같이 선 적이 아직 없다.
성능 축은 거둔다 — 3주차에 「성능 축을 판정한다」고 말했지만, 시간이 없어서가 아니라 어떻게 재야 하는지 모르기 때문이다. 같은 하네스로 세 번 재고 못 갈랐고, 표본을 늘리려 좌석을 늘리면 경합이 사라진다.
정합성 축은 쟀다. 남은 것은 #209 수정과 전 구간 한 번 기동. 발표까지 78일.
1차와 2차의 차이 — 가장 물으실 자리다.
1차 (성능 축)              2차 (정합성 축)
무엇을   다섯 전략을 비교            한 전략으로 밟기만
지표     p95 · TPS                 위반 건수 0이냐 아니냐
잡음     크다 — 같은 조건 재측정이   없다 — 0이냐 아니냐엔 분산이 없다
18~41ms로 흔들린다
결론     p95로는 전략을 구분 못 한다  이 조건에서 안 깨졌다
★ «깨질 수 없다»가 아니라 «이 조건에서 안 깨졌다»다. 경합 발현은 확률이고, 0은 그것을 관측하지 못했다는 뜻이다. 이 구분을 먼저 말하면 질문이 안 나온다.
```

### 슬라이드 11

```
마무리 — 질문 받는 자리.
나올 만한 것: (1) 7번에서 올렸다면서 왜 90%냐 → 엣지와 오리진을 따로 쟀다. (2) 성능 축을 왜 접나 → 재는 방법이 없다, 세 번 재고 못 갈랐다. (3) AWS 지금 떠 있나 → 확인 후 내렸다, 비용 때문이며 시연 1회 ~$0.45로 잡아 뒀다.
```
