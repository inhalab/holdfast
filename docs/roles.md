\# 역할 분담



| | 최건 (@Asirante) | 박태준 (@F3ZLoV) |

|---|---|---|

| \*\*핵심 역할\*\* | 동시성 코어 — 설계·구현·측정 | 인프라 + 도메인 기능 |

| \*\*1개월차\*\* | 요구사항 추적표, ERD, 상태 전이도, 동시성 설계서, 예약 API 계약 확정 | AWS 개발 환경, 로컬 Docker Compose(앱·DB·Redis), CI 확장, k6 실행 환경 |

| \*\*2개월차\*\* | 조회·선점·확정 기본 흐름(락 없는 베이스라인), 부하 테스트로 실패 재현·기록 | 다중 인스턴스 배포(ALB + 앱 2대), Redis 구성, 좌석맵·선점 UI |

| \*\*3개월차\*\* | 락 전략 4종 구현, 전략별 비교 측정, 만료·취소·멱등성 | Mock PG(실패 주입 포함), 예약 조회 화면, 모니터링·로그 수집 |

| \*\*4개월차\*\* | 최종 측정, 비교표 작성, 발표 자료 | QR 티켓·검표, 알림 Outbox, 관리자 화면 |

| \*\*공통\*\* | 요구사항 추적표, ERD, 상태 전이도, 최종 발표 | 좌: 문서 초안 / 우: 리뷰 |



\## 경계선



\*\*선점 로직은 최건, 선점 UI는 박태준.\*\* 이 분담의 핵심이다.



홀드 획득과 예약 확정은 같은 임계 구역을 공유하고, TTL 만료와 확정의 경합도 여기서 터진다. 두 사람이 나눠 가지면 가장 중요한 코드에서 계속 충돌한다. 서버 로직은 최건이 통째로 갖고, 그 앞단 화면만 박태준이 붙인다.



\## 디렉토리별 담당



```

api/

&#x20; reservation/     좌석 선점, 예약 확정, 취소, 만료      → 최건

&#x20; seat/            좌석·회차 조회                        → 최건

&#x20; payment/         Mock PG                               → 박태준

&#x20; ticket/          QR 발급, 검표                         → 박태준

&#x20; notification/    Outbox                                → 박태준

&#x20; admin/           관리자 기능                           → 박태준

web/               전체 화면                             → 박태준

infra/             Terraform, 배포                       → 박태준

load-test/         k6 시나리오                           → 최건

```



접근 권한이 아니라 리뷰 책임의 구분이다. 상대 영역도 필요하면 손대되, PR에서 담당자가 리뷰어로 붙는다. 실제 패키지 구조가 확정되면 `.github/CODEOWNERS`와 함께 갱신한다.



\## 순서상 중요한 것



\*\*박태준의 2개월차 다중 인스턴스 배포가 최건의 부하 테스트를 막는다.\*\* 단일 인스턴스로만 측정하면 분산락을 왜 쓰는지 증명할 수 없다. 2개월차 중반까지는 앱 2대가 뜨는 상태를 만들어야 한다.



\*\*최건은 1개월차에 API 계약을 먼저 확정해야 한다.\*\* 박태준이 UI를 만들려면 스펙이 있어야 하고, 없으면 계속 기다리게 된다. `POST /holds`, `POST /reservations`, `DELETE /holds/{id}` 정도의 요청·응답 스키마를 M1 안에 못박는다.



\*\*Mock PG는 박태준이 만들되, 인터페이스는 최건이 정한다.\*\* 부하 테스트에서 타임아웃·지연·승인 누락을 주입해야 하므로 최건이 필요한 형태를 먼저 알려주는 것이 맞다.



\## 발표에서의 기여 구분



\- 최건 — 동시성 문제를 재현하고 네 가지 전략으로 제거한 과정

\- 박태준 — 그 측정이 성립하도록 다중 인스턴스 환경을 만들고 도메인 기능을 붙인 과정

